// Reverssing the string through strings

#include <iostream>
#include <math.h>
using namespace std;
int main()
{
  string str = "Apna College";

  reverse(str.begin(), str.end());

  cout << str << endl;
  
  return 0;
}
